{
  "name": "thunderbird-translator",
  "version": "1.7.0",
  "description": "Thunderbird email translation extension",
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "build": "zip -r thunderbird-translator.xpi manifest.json background.js content/ options/ icons/ _locales/ --exclude '*.test.js'"
  },
  "devDependencies": {
    "fast-check": "^3.22.0",
    "vitest": "^2.0.0"
  }
}
